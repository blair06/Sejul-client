import React from 'react'
import * as API from '../../api';

const MainView = () => {
    // API.Summary.fetchAll();
    return (
        <div>
            <p>
                Hello
        </p>
        </div>
    )
}

export default MainView;
